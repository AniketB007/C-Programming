#include<stdio.h>
#include<conio.h>
int main()
{
	int r,c,n;
	for (r=1;r<=1;r++)
	{
		for (c=5;c>=1;c++)
		{
			if(c<=r)
			printf("%d",c);
			else
			printf("*");
		}
		printf("\n");
	}
}